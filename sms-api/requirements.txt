fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0

# Code quality / linting (used by lint-on-edit PostToolUse hook)
ruff>=0.4.0          # Python linter — very fast (~30ms/file)
shellcheck-py>=0.9.0 # Shell linter wrapper (installs real shellcheck binary)
